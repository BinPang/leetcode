package main

func majorityElement(nums []int) []int {
	r := make([]int, 0)
	n := len(nums)/3
	tmp := make(map[int]int, len(nums))

	for k, v := range nums {
		tmp[v] += 1
		if tmp[v] ==  {

		}
	}
}
